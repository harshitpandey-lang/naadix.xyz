import { SectionHeading } from "./section-heading";
export function AboutSection({ summary }: { summary: string[] }) { return <section id="about" className="section container"><div className="grid gap-12 lg:grid-cols-[.8fr_1.2fr]"><div><SectionHeading eyebrow="01 / About" title="A practical curiosity for how things work." /><div className="flex flex-wrap gap-2">{["AI + Robotics", "STEM Education", "VLSI / Embedded Systems", "CAD / Engineering"].map((item) => <span key={item} className="rounded-full border border-[var(--line)] px-3 py-1.5 text-xs font-semibold text-[var(--muted)]">{item}</span>)}</div></div><div className="space-y-5 text-[1.04rem] leading-8 text-[var(--muted)]">{summary.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div></div></section>; }
