import { Mail, MapPin, Phone } from "lucide-react";
import type { Profile } from "@/src/types/profile";
import { SectionHeading } from "./section-heading";
export function ContactSection({ profile }: { profile: Profile }) { return <section className="section bg-[var(--navy)] text-white"><div className="container"><SectionHeading eyebrow="08 / Contact" title="Let’s start a conversation." intro="For teaching, technology, and professional opportunities." /><div className="grid gap-8 border-t border-white/20 pt-8 md:grid-cols-3"><a href={`mailto:${profile.email}`} className="group"><Mail size={20} className="text-[#8cbde0]" aria-hidden="true"/><p className="mt-4 text-xs font-bold uppercase tracking-wider text-white/55">Email</p><p className="mt-2 font-medium group-hover:text-[#8cbde0]">{profile.email}</p></a><div><MapPin size={20} className="text-[#8cbde0]" aria-hidden="true"/><p className="mt-4 text-xs font-bold uppercase tracking-wider text-white/55">Location</p><p className="mt-2 font-medium">{profile.contactLocation}</p></div><details className="group"><summary className="cursor-pointer list-none"><Phone size={20} className="text-[#8cbde0]" aria-hidden="true"/><span className="mt-4 block text-xs font-bold uppercase tracking-wider text-white/55">Phone</span><span className="mt-2 block font-medium text-white/75 group-open:hidden">Show work contact</span><span className="mt-2 hidden font-medium group-open:block">{profile.phone}</span></summary></details></div></div></section>; }
