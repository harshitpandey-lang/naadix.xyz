import type { Education } from "@/src/types/profile";
import { SectionHeading } from "./section-heading";
export function EducationSection({ education }: { education: Education[] }) { return <section id="education" className="section container"><SectionHeading eyebrow="07 / Education" title="The foundation." /><div className="grid gap-x-12 lg:grid-cols-2">{education.map((item) => <article key={item.institution} className="border-t border-[var(--line)] py-7"><h3 className="text-xl font-bold tracking-tight">{item.institution}</h3>{item.qualification && <p className="mt-3 font-medium">{item.qualification}</p>}{item.field && <p className="mt-1 text-sm text-[var(--muted)]">{item.field}</p>}{item.description && <p className="mt-3 text-sm text-[var(--muted)]">{item.description}</p>}{item.date && <p className="mt-5 text-xs font-bold uppercase tracking-wider text-[var(--accent)]">{item.date}</p>}</article>)}</div></section>; }
