import { MapPin } from "lucide-react";
import type { Experience } from "@/src/types/profile";
import { SectionHeading } from "./section-heading";
export function ExperienceSection({ experiences }: { experiences: Experience[] }) { return <section id="experience" className="section container"><SectionHeading eyebrow="03 / Experience" title="Teaching, technology, and mentorship." /><ol className="relative border-l border-[var(--line)] pl-7 sm:pl-10">{experiences.map((item) => <li key={`${item.company}-${item.role}`} className="relative pb-12 last:pb-0"><span className={`absolute -left-[2.06rem] top-1.5 h-3.5 w-3.5 rounded-full border-4 border-[var(--paper)] sm:-left-[2.8rem] ${item.current ? "bg-[var(--accent)]" : "bg-[#aab7bf]"}`} /><article className="rounded-lg border border-transparent p-1 transition hover:border-[var(--line)] hover:bg-white sm:p-5"><div className="flex flex-col justify-between gap-2 sm:flex-row"><div><p className="text-lg font-bold">{item.company}</p><h3 className="mt-1 text-xl font-semibold tracking-tight">{item.role}</h3></div><p className={`w-fit rounded-full px-3 py-1 text-xs font-bold ${item.current ? "bg-[#dcecf7] text-[#155182]" : "bg-[#edf0f1] text-[var(--muted)]"}`}>{item.date}</p></div>{item.location && <p className="mt-4 flex items-center gap-2 text-sm text-[var(--muted)]"><MapPin size={15} aria-hidden="true" />{item.location}</p>}{item.description && <ul className="mt-5 space-y-2 border-t border-[var(--line)] pt-4 text-sm leading-6 text-[var(--muted)]">{item.description.map((line) => <li key={line}>— {line}</li>)}</ul>}</article></li>)}</ol></section>; }
