import type { FeaturedItem } from "@/src/types/profile";
import { SectionHeading } from "./section-heading";
export function FeaturedSection({ items }: { items: FeaturedItem[] }) { return <section id="featured" className="section rule"><div className="container"><SectionHeading eyebrow="02 / Featured" title="Work in focus." intro="A considered space for projects, moments, and things worth sharing." />{items.length === 0 ? <div className="border border-dashed border-[#b8c6ce] bg-[#f2f5f5] p-10 sm:p-14"><p className="text-lg font-semibold">Featured work and moments will appear here.</p><p className="mt-2 text-sm text-[var(--muted)]">This space is prepared for future projects, media, events, and achievements.</p></div> : <div className="grid gap-5 md:grid-cols-2">{items.map((item) => <article key={item.id} className="card p-6"><p className="eyebrow">{item.type}</p><h3 className="mt-3 text-xl font-bold">{item.title}</h3>{item.description && <p className="mt-2 text-[var(--muted)]">{item.description}</p>}</article>)}</div>}</div></section>; }
