"use client";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useState } from "react";
const links = [{ label:"About", href:"#about" }, { label:"Experience", href:"#experience" }, { label:"Skills", href:"#skills" }, { label:"Education", href:"#education" }, { label:"Featured", href:"#featured" }];
export function SiteNavigation() { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-40 border-b border-[var(--line)] bg-[#f8f8f6e8] backdrop-blur"><nav className="container flex h-16 items-center justify-between" aria-label="Primary navigation"><a href="#top" className="text-sm font-black tracking-[.18em]">NAADIX</a><div className="hidden items-center gap-6 md:flex">{links.map((link) => <a className="text-sm text-[var(--muted)] transition hover:text-[var(--ink)]" href={link.href} key={link.label}>{link.label}</a>)}</div><Link href="/login" className="hidden rounded-md bg-[var(--navy)] px-4 py-2 text-sm font-semibold text-white transition hover:bg-[var(--accent)] sm:block">Personal HQ</Link><button onClick={() => setOpen(!open)} className="p-2 md:hidden" aria-label="Toggle navigation" aria-controls="mobile-navigation" aria-expanded={open}>{open ? <X /> : <Menu />}</button></nav>{open && <div id="mobile-navigation" className="border-t border-[var(--line)] bg-[var(--paper)] px-4 pb-5 pt-3 md:hidden"><div className="container grid gap-1">{links.map((link) => <a onClick={() => setOpen(false)} className="py-2 text-sm" href={link.href} key={link.label}>{link.label}</a>)}<Link href="/login" onClick={() => setOpen(false)} className="mt-2 w-fit rounded-md bg-[var(--navy)] px-4 py-2 text-sm font-semibold text-white">Personal HQ</Link></div></div>}</header>; }
